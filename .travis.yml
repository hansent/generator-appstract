language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-appstract' ]; then cd .. && eval "mv $currentfolder generator-appstract" && cd generator-appstract; fi

